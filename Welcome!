<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>刘淮曦 | 工程师作品集</title>
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2290%22>⚡</text></svg>">
    <style>
        /* ==================== Reset & Basic Styles ==================== */
        :root {
            --color-bg: #f8f8f8;
            --color-text: #333;
            --color-accent: #007bff; /* Apple Blue */
            --color-border: #e0e0e0;
            --font-mono: 'SFMono-Regular', Consolas, 'Liberation Mono', Menlo, Courier, monospace;
            --font-sans: 'Inter', 'Helvetica Neue', Arial, sans-serif; /* Modern, clean sans-serif */
        }

        /* 引入 Google Fonts 的 Inter 字体 */
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;700&display=swap');

        body {
            margin: 0;
            padding: 0;
            font-family: var(--font-sans);
            color: var(--color-text);
            background-color: var(--color-bg);
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            overflow: hidden; /* Prevent scrollbar from welcome animation */
            transition: background-color 0.3s ease;
        }

        /* ==================== Container & Layout ==================== */
        .container {
            width: 100%;
            max-width: 800px; /* MUJI-like width constraint */
            padding: 40px;
            box-sizing: border-box;
            text-align: center;
            background: #ffffff;
            border-radius: 12px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.08); /* Soft shadow */
            position: relative;
            z-index: 10;
        }

        /* ==================== Welcome Animation ==================== */
        .welcome-display {
            font-family: var(--font-mono);
            font-size: 3.5em; /* Larger, impactful */
            font-weight: 500;
            color: var(--color-accent);
            min-height: 1.2em; /* Ensure consistent height */
            display: flex;
            justify-content: center;
            align-items: center;
            margin-bottom: 40px;
            position: relative;
            overflow: hidden; /* Hide overflowing text during animation */
            opacity: 0.9;
        }

        .welcome-text {
            position: absolute;
            opacity: 0;
            transform: translateY(20px);
            animation: showWelcome 8s infinite; /* Loop through welcomes */
        }

        /* Each welcome message has a specific animation delay */
        .welcome-text:nth-child(1) { animation-delay: 0s; } /* Welcome */
        .welcome-text:nth-child(2) { animation-delay: 2s; } /* 欢迎 */
        .welcome-text:nth-child(3) { animation-delay: 4s; } /* Bienvenue */
        .welcome-text:nth-child(4) { animation-delay: 6s; } /* いらっしゃいませ */


        @keyframes showWelcome {
            0% { opacity: 0; transform: translateY(20px); }
            5% { opacity: 1; transform: translateY(0px); }
            20% { opacity: 1; transform: translateY(0px); }
            25% { opacity: 0; transform: translateY(-20px); }
            100% { opacity: 0; transform: translateY(20px); }
        }

        /* ==================== Header & Profile ==================== */
        .header {
            margin-bottom: 30px;
        }

        .profile-icon {
            font-size: 5em;
            line-height: 1;
            margin-bottom: 10px;
            color: var(--color-text); /* Neutral icon color */
        }

        .site-title {
            font-family: var(--font-sans);
            font-size: 2.2em;
            font-weight: 700;
            margin: 0;
            color: var(--color-text);
        }

        .tagline {
            font-size: 1.1em;
            color: #666;
            margin-top: 5px;
            font-weight: 300; /* Lighter weight for MUJI feel */
        }

        /* ==================== Navigation Links ==================== */
        .nav-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); /* Responsive grid */
            gap: 20px;
            margin-top: 40px;
        }

        .nav-item {
            background-color: var(--color-bg);
            border: 1px solid var(--color-border);
            border-radius: 8px;
            padding: 25px 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            text-decoration: none;
            color: var(--color-text);
            transition: all 0.2s ease;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.05);
        }

        .nav-item:hover {
            border-color: var(--color-accent);
            transform: translateY(-5px);
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
        }

        .nav-icon {
            font-size: 2.5em;
            margin-bottom: 15px;
            line-height: 1;
        }

        .nav-title {
            font-size: 1.2em;
            font-weight: 500;
            margin-bottom: 5px;
        }

        .nav-description {
            font-size: 0.9em;
            color: #888;
        }

        /* ==================== Footer ==================== */
        .footer {
            margin-top: 60px;
            font-size: 0.9em;
            color: #999;
            font-family: var(--font-mono);
        }
        .footer a {
            color: var(--color-text);
            text-decoration: none;
            border-bottom: 1px dashed var(--color-border);
            transition: color 0.2s ease;
        }
        .footer a:hover {
            color: var(--color-accent);
            border-bottom-color: var(--color-accent);
        }

        /* ==================== Responsive Adjustments ==================== */
        @media (max-width: 600px) {
            .welcome-display {
                font-size: 2.5em;
            }
            .site-title {
                font-size: 1.8em;
            }
            .tagline {
                font-size: 1em;
            }
            .nav-grid {
                grid-template-columns: 1fr; /* Stack on small screens */
            }
            .container {
                padding: 20px;
                margin: 20px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="welcome-display">
            <span class="welcome-text">Welcome</span>
            <span class="welcome-text">欢迎</span>
            <span class="welcome-text">Bienvenue</span>
            <span class="welcome-text">いらっしゃいませ</span>
        </div>

        <header class="header">
            <div class="profile-icon">⚡</div>
            <h1 class="site-title">刘淮曦</h1>
            <p class="tagline">电子科学与技术 | AI爱好者 | 代码与创意</p>
        </header>

        <nav class="nav-grid">
            <a href="nailong-story/" class="nav-item">
                <span class="nav-icon">🐲</span>
                <span class="nav-title">《奶龙的进化论》</span>
                <span class="nav-description">我的第一个互动漫画项目</span>
            </a>
            <a href="project-alpha/" class="nav-item">
                <span class="nav-icon">🔬</span>
                <span class="nav-title">Project Alpha</span>
                <span class="nav-description">关于AI图像识别的实验</span>
            </a>
            <a href="project-beta/" class="nav-item">
                <span class="nav-icon">💻</span>
                <span class="nav-title">Project Beta</span>
                <span class="nav-description">Python数据可视化研究</span>
            </a>
            <a href="#" class="nav-item">
                <span class="nav-icon">📝</span>
                <span class="nav-title">我的技术博客</span>
                <span class="nav-description">学习笔记与思考</span>
            </a>
            <a href="#" class="nav-item">
                <span class="nav-icon">📧</span>
                <span class="nav-title">联系我</span>
                <span class="nav-description">合作与交流</span>
            </a>
        </nav>

        <footer class="footer">
            &copy; 2023 刘淮曦. Driven by curiosity.
        </footer>
    </div>
</body>
</html>
